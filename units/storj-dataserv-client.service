[Unit]
Description=Storj dataserv-client
IgnoreOnIsolate=true

[Service]
Environment="MAX_SIZE=100GB"
ExecStart=/usr/bin/dataserv-client --url=http://switch.driveshare.org --max_size=${MAX_SIZE} farm
Restart=always

[Install]
WantedBy=default.target
